services:
  - type: web
    name: obrasignal
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn preload:APP
    envVars:
      - key: POLL_SECONDS
        value: "300"
