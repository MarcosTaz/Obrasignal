services:
  - type: web
    name: obrasignal
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:APP
    envVars:
      - key: POLL_SECONDS
        value: "300"
