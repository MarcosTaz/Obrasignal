ObraSignal web deployment marker

This file intentionally triggers the GitHub Pages production build.
The deployed page is stamped by the workflow with the exact source commit SHA; this marker is not the source-of-truth for the deployed version.
