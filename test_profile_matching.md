# ObraSignal acceptance scenarios

1. Strong match: structures metálicas / pavilhão, ES, CPV 4522, EUR 850k -> ALERTA MÁXIMO.
2. Excluded service: arquitetura / fiscalização -> below alert threshold.
3. Country mismatch: DE receives no preferred-country bonus versus ES.
4. Same source + external id -> same event identity; different ids -> distinct events.
